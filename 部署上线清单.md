# CL 知识库 · 上线对照清单

> 目标：用 **GitHub Pages** 把网站发布成一个手机/电脑随时能打开的永久网址 `https://你的用户名.github.io/ai-kb/`
> 预计耗时：第一次约 10-15 分钟（大部分时间在等注册和构建）
> 费用：全程免费

---

## ⚠️ 开始前的两个铁律（违反会白做）

1. **仓库名必须叫 `ai-kb`**（小写，连字符，不能改别的）。网站里面写死了这个路径，名字不对全站白屏。
2. **`node_modules/`、`dist/` 千万不要手动传**——已经在 `.gitignore` 里，GitHub Desktop 会自动跳过，传上去也白传。

---

## ☑ 第 1 步：注册 GitHub（免费）

- [ ] 打开 <https://github.com> → 点右上角 **Sign up**
- [ ] 用邮箱注册，记好用户名（网址里要用，建议简短好记，比如 `cl2026`）
- [ ] 验证邮箱（进邮箱点那封确认信）

---

## ☑ 第 2 步：新建仓库

- [ ] 登录后，右上角 `+` → **New repository**
- [ ] Repository name 填：**`ai-kb`**（⚠️ 必须一模一样）
- [ ] 选 **Public**
- [ ] 下面三个勾（Add README / Add .gitignore / Choose license）**都不要勾**
- [ ] 点 **Create repository**

---

## ☑ 第 3 步：安装 GitHub Desktop（免费，专门解决"160 个文件一次传完"）

- [ ] 下载 <https://desktop.github.com> → 安装 → 打开
- [ ] 用刚才的账号登录
- [ ] **File → Clone repository** → 选 `ai-kb` → 选个本地位置（比如桌面）→ **Clone**
- [ ] 记下这个空文件夹的位置（后面要把网站文件复制进去）

---

## ☑ 第 4 步：把网站文件复制进去并上传

- [ ] 打开刚才克隆出来的**空文件夹**
- [ ] 把下面这些从 `cl-kb` 复制进去（直接全选复制即可）：

```
要复制的内容（共 7 项）：
  .github/        （整个文件夹，注意开头的点）
  docs/           （整个文件夹）
  scripts/        （整个文件夹）
  tools/          （整个文件夹）
  package.json
  .gitignore
  README.md
```

- [ ] 回到 **GitHub Desktop**，会看到约 153 个待提交文件
- [ ] 左下角 Summary 写：`init`（随便写都行）
- [ ] 点 **Commit to main**
- [ ] 再点 **Push origin**（第一次会显示 **Publish branch**，点它）

> 等上传跑完（进度条 100%）。这一步如果卡住，多半是网络，重试即可。

---

## ☑ 第 5 步：开启 Pages（让网站能被访问）

- [ ] 回到 github.com 上的 `ai-kb` 仓库
- [ ] 点顶部 **Settings** → 左侧 **Pages**
- [ ] Source 选 **GitHub Actions**（不是 Deploy from a branch）
- [ ] 点顶部 **Actions** 标签，会看到两个自动跑的任务：
  - `每日抓取 AI 热点`（如果没自动跑，点进去手动 Run workflow）
  - `deploy`（抓取完会自动被触发）
- [ ] 等它们**变绿**（约 2 分钟，第一次可能稍久）

---

## ☑ 第 6 步：拿到你的网址

- [ ] 构建变绿后，网址就是：
  ```
  https://你的用户名.github.io/ai-kb/
  ```
- [ ] 用手机和电脑各打开一次，确认能看
- [ ] 试试顶部搜索框，搜「RAG」或「Claude」

---

## 🔧 上线后建议你顺手做的（可选但推荐）

- [ ] **改用户名**：打开 `cl-kb/docs/.vitepress/config.mjs`，把里面的 `你的用户名` 换成真正的 GitHub 用户名，提交。之后每页底部会出现「在 GitHub 上编辑这一页」按钮——这是你以后加内容最常用的入口。
- [ ] **开 AI 自动翻译热点**：仓库 `Settings` → `Secrets and variables` → `Actions` → `New repository secret` → Name 填 `OPENAI_API_KEY`，Secret 填你的 Key（推荐去 <https://platform.deepseek.com> 免费领）→ 保存。下次抓新闻就会自动翻成中文、打标签。
- [ ] **换主题色**：`cl-kb/docs/.vitepress/config.mjs` 里有说明。

---

## 📅 日常怎么用

| 想做什么 | 怎么做 |
|---|---|
| 看内容 | 打开 `https://你的用户名.github.io/ai-kb/`，顶部搜索框搜任意词 |
| 每天自动更新 | 每天早上 7 点（北京时间）自动抓 9 个源的热点，不用管 |
| 加自己的笔记/术语 | GitHub 网页进 `docs/` → 找到文件 → 点铅笔图标改 → 拉到底 `Commit changes` → 等 1-2 分钟更新 |
| 标记学习任务完成 | 打开任务文件，把 `done: false` 改成 `done: true`，提交，进度条自动更新 |
| 加新术语 | 在 `docs/glossary/` 新建 `.md` 文件，照着已有的格式写即可，左侧栏自动出现 |

---

## 🆘 卡住了怎么办

做到哪一步卡住，就把那一步的**截图或报错文字**发给我，我一步步陪你过。
常见卡点：
- **Clone 不到仓库** → 确认 GitHub Desktop 已登录、仓库是 Public
- **Pages 里没有 GitHub Actions 选项** → 确认仓库名是 `ai-kb`、且已成功 Push 过代码
- **网址打不开/白屏** → 等 Actions 全绿后再试；白屏多半是仓库名不对
